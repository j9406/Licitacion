@model IEnumerable<PCILicitacion.Model.LicitacionAdjudicacion>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Licitacion.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LicitacionOfertaPartida.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LicitacionPartida.Observaciones)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Licitante.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Licitacion.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LicitacionOfertaPartida.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LicitacionPartida.Observaciones)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Licitante.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
